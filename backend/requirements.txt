# Boston Pulse Backend — Dependencies

# API framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
pydantic-settings==2.2.1

# LLM — Gemini (primary)
google-generativeai==0.5.4

# GCP
google-cloud-storage==2.16.0
google-cloud-firestore==2.16.0

# Embeddings — HuggingFace (free, local)
sentence-transformers==2.7.0

# Vector DB — ChromaDB (latest fixes numpy 2.0 issue)
chromadb>=1.0.0

# Data
pandas==2.2.2
pyarrow>=17.0.0

# NumPy pinned to avoid ChromaDB conflict
numpy>=2.0.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
